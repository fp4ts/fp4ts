{
  "name": "@fp4ts/collections-perf",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist && rm -f *.tsbuildinfo && rm -rf node_modules",
    "compile": "yarn tsc --build tsconfig.build.json"
  },
  "dependencies": {
    "@fp4ts/cats": "*",
    "@fp4ts/collections-core": "*",
    "@fp4ts/core": "*",
    "benny": "^3.7.1"
  },
  "devDependencies": {
    "typescript": "5.0.2"
  }
}
